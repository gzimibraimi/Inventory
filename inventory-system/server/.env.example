PORT=5000
DATABASE_URL=postgres://user:password@localhost:5432/inventory_db
# Use this file as a template and copy it to .env before running the server.
# Example in PowerShell:
# cp .env.example .env
